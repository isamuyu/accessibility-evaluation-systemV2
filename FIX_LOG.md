# 修复说明

## 问题发现
系统存在建筑类型与参评维度不匹配的问题：

1. **住宅类建筑**不应评价Q2（公共无障碍服务），但控制项章节选项包含Q2
2. **不含住宿的公共建筑**不应评价Q3（无障碍住宿），但设施类别包含居室、无障碍卫生间
3. 后端缺少校验，可能通过API直接添加不参评的章节评分

## 修复内容

### 前端修复

1. **ControlItemsPanel（控制项面板）**
   - 接收 `applicableChapters` 参数
   - 章节下拉框只显示该建筑类型参评的章节
   - 提交时校验章节是否属于参评维度
   - 列表只显示参评章节的控制项

2. **FacilityScoresPanel（设施评分面板）**
   - 接收 `applicableChapters` 参数
   - 根据建筑类型过滤设施类别：
     - 住宅类：只显示Q1设施（通道、坡道、电梯等）和Q3设施（居室、无障碍卫生间）
     - 不含住宿公共建筑：只显示Q1和Q2设施，不显示Q3设施
   
3. **SystemScoresPanel（系统评分面板）**
   - 维度进度卡只显示参评章节
   - 章节筛选下拉框只显示参评章节

### 建筑类型与参评维度对照表

| 建筑类型 | 参评维度 | 不参评 |
|---------|---------|--------|
| 住宅类居住建筑 | Q1, Q3, Q4, Q5 | **Q2** |
| 非住宅类居住建筑 | Q1, Q2, Q3, Q4, Q5 | 无 |
| 含住宿功能公共建筑 | Q1, Q2, Q3, Q4, Q5 | 无 |
| 不含住宿功能公共建筑 | Q1, Q2, Q4, Q5 | **Q3** |

### 后端逻辑
后端计算逻辑原本已正确处理：
- `Q2` 只在非住宅类、含住宿公建、不含住宿公建时计算
- `Q3` 只在住宅类、非住宅类、含住宿公建时计算
- 不含住宿公建不计算Q3

## 验证方式

1. 创建住宅类建筑 → 控制项章节不应看到"Q2-公共服务"
2. 创建不含住宿公共建筑 → 设施类别不应看到"居室"和"无障碍卫生间"
3. 所有建筑类型都应正确显示各自的参评维度